mixin svg(name, addClasses)
	- var svgClass = 'svg-sprite svg-sprite-'+name
	- var classesList = '';
	if addClasses
		each addClass in addClasses
			- classesList = classesList+' '+addClass
	- svgClass = svgClass+classesList
	svg(class= svgClass)
		use(xlink:href="#"+name)